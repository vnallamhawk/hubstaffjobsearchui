const hostName = window&&window.location&&window.location.hostname;
var backEndHost = "https://hubstaffapi.herokuapp.com"
//var backEndHost = "http://localhost:8080"
export const root = `${backEndHost}`